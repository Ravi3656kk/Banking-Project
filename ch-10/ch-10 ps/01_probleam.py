class programmer:
      company ="miscrosoft"
      def __init__(self, name,salary,pin):
            self.name = name
            self.salary = salary
            self.pin = pin


p = programmer("sharad bhole",1000000,250004)
print(p.name, p.salary, p.pin, p.company)

r = programmer("manjulika", 1200000, 110045)
print(r.name, r.salary, r.pin, r.company)

